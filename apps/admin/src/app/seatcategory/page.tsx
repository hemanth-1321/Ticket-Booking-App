"use client";
import { SeatCategories } from "@/components/SeatCategories";
import React, { useState } from "react";

const page = () => {
  return (
    <div>
      <SeatCategories />
    </div>
  );
};

export default page;
